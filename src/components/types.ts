export type ClassNameProps = {
  className?: string
}

export enum Size {
  sm = 'sm',
  md = 'md',
  lg = 'lg',
}

export enum Tone {
  primary = 'primary',
  neutral = 'neutral',
  warning = 'warning',
  critical = 'critical',
  success = 'success',
  informative = 'informative',
}
